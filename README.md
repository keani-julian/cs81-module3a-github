# cs81-module3a-github
Assignment 3A: Git &amp; GitHub Repo Setup
# Function Journal – Module 3A Assignment
This repository contains my journal of JavaScript functions to practice Chapter 3 concepts.

## Functions
- **greet(name)**: Returns a greeting string for the provided name.
- **isEvenOrOdd(number)**: Determines if a number is even or odd.
- **convertMinutesToSeconds(minutes)**: Converts minutes to seconds.
- **canRead(age)**: Returns true if age is 3 or older.
- **countDown(number)**: Recursively counts down from a number to 0, ending with "Done!".
- **maxOfTwo(a, b)**: Returns the larger of two numbers.